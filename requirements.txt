# 📦 requirements-full.txt (최종 백엔드 패키지 목록)
# 설치 명령어 -> pip install -r requirements-full.txt

flask==2.0.1
flask-cors==3.0.10
requests==2.31.0
gunicorn==21.2.0         # 선택적, 배포 환경용
werkzeug==2.0.0          # flask 의존성
numpy>=1.21.0            # TF-IDF 유사도 계산
scikit-learn>=0.24.0     # TfidfVectorizer
python-dotenv>=0.21.0    # .env 환경변수 지원
