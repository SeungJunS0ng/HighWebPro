# 📦 requirements-full.txt (최종 백엔드 패키지 목록)
# 설치 명령어 -> pip install -r requirements.txt
# npm install도 필수!

flask==2.0.1             # Flask 웹 서버
flask-cors==3.0.10       # CORS 처리
requests==2.31.0         # 뉴스 페이지 요청
beautifulsoup4==4.12.3   # HTML 파싱
numpy>=1.21.0            # TF-IDF 유사도 계산
scikit-learn>=0.24.0     # TfidfVectorizer
python-dotenv>=0.21.0    # .env 환경변수 관리

